# Grafana PostgreSQL 데이터 소스 프로비저닝
# FeedlyAI 데이터베이스 메트릭 조회용

apiVersion: 1

datasources:
  - name: FeedlyAI PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: feedlyai
    user: feedlyai
    secureJsonData:
      password: feedlyai_dev_password_74154
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
      maxOpenConns: 100
      maxIdleConns: 100
      connMaxLifetime: 14400
    isDefault: false
    editable: true

